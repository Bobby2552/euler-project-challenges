#include <iostream>
#include <math.h>

using namespace std;

int main () {
  int a, b, c;
  cout << "Base : ";
  cin >> a;
  cout << "Exponent : ";
  cin >> b;
  cout << "Modulo : ";
  cin >> c;

  int pows [100];
  for (int i = 0; i < 100; i++) {
    pows[i] = pow(2, i);
  }

  int curExp = 0;
  for (int i = 0; i < b; i++ {
    
  }
}
