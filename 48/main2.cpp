#include <iostream>
#include <math.h>

using namespace std;

int main () {
  unsigned long long sum;

  //  for (int i = 1; i < 1001; i++) {
  //sum += (unsigned long long)
  //  }
  sum = pow(1000, 1000);
  cout << sum << "\n";
  cout << pow(1000, 1000);
}
